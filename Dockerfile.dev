# 开发环境 Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装开发依赖
COPY package*.json ./
RUN npm install

# 复制源代码
COPY . .

# 创建必要的目录
RUN mkdir -p /app/logs /app/database

# 暴露端口
EXPOSE 3000

# 开发模式启动
CMD ["npm", "run", "dev"]